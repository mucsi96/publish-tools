[project]
name = "publish_tools"
version = "0.0.1"
authors = [
  { name = "Igor Bari", email = "mucsi96@gmail.com" },
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
